提交本文件前，请确认文件中不再有任何 TODO 占位符，并删除这段说明。

独占指针
--------

问题 1：相比手动调用 `new` 和 `delete`，使用 RAII 管理内存有哪些好处？请列出一到两点。
回答 1：TODO

问题 2：实现 `unique_ptr` 的移动语义时，例如在移动构造函数 `unique_ptr(unique_ptr&& other)` 中，函数结束前必须把 `other` 的底层指针设为 `nullptr`。请用自己的话解释，如果不这样做会产生什么问题。
回答 2：TODO

问题 3：通过 RAII 递归释放对短链表很有效，但长链表可能出现问题。为什么？提示：递归函数调用栈的深度有什么限制？
回答 3：TODO

问题 4：此处 `std::move` 做了什么？为什么在这里使用 `std::move` 和移动语义是安全的？
回答 4：TODO
